import PubSub from 'pubsubjs';

let display = (function() {

    let sideBar = document.getElementById('sidebar');
    let projectList = sideBar.getElementById('projectlist');
    let addNewProjectBtn = sidebar.getElementById('addnewproject');

    let mainView = document.getElementById('main-content');
    let projectTitle = mainView.getElementById('projectTitle');
    let todoList = mainView.getElementById('todolist');
    let addTodoBtn = mainView.getElementById('add-todo');
    let toggleSideBarBtn = mainView.getElementById('togglesidebar');

})();